# 变量和条件功能文档 🎯

## 📋 概述

本次更新添加了工作流中最重要的基础功能：
- ✅ **自定义变量系统** - 全局变量和局部变量
- ✅ **Set Variable节点** - 设置变量值
- ✅ **Get Variable节点** - 获取变量值
- ✅ **If-Else节点** - 完整的条件分支（支持else if）
- ✅ **变量管理面板** - 可视化管理工作流变量

---

## 🎯 新增节点

### 1. Set Variable（设置变量）节点

**用途：** 在工作流中设置变量的值

**配置项：**

#### 变量名称
- 只能包含字母、数字和下划线
- 不能以数字开头
- 示例：`user_name`, `score_threshold`, `api_key`

#### 变量作用域
- **工作流变量（全局）**：整个工作流都可以访问
- **局部变量（当前分支）**：只在当前分支内有效

#### 变量值来源
1. **静态值**
   - 直接输入固定的值
   - 支持字符串、数字、JSON对象
   - 示例：
     ```
     "Hello World"
     123
     {"key": "value"}
     ```

2. **输入数据**
   - 从上一个节点的输出中提取
   - 使用路径语法：`data.user.name`
   - 示例：
     ```
     input.response.content
     input.data[0].id
     ```

3. **表达式**
   - 使用JavaScript表达式计算
   - 可以引用其他变量
   - 示例：
     ```javascript
     input.count * 2 + 10
     $threshold + input.score
     ```

**使用场景：**
- 保存API响应数据
- 计算中间结果
- 存储用户输入
- 设置配置参数

---

### 2. Get Variable（获取变量）节点

**用途：** 从工作流中获取变量的值

**配置项：**

#### 变量名称
- 要获取的变量名
- 必须是已经设置过的变量

#### 默认值（可选）
- 当变量不存在时使用的默认值
- 避免工作流因变量不存在而失败

#### 输出字段名
- 输出数据中的字段名
- 默认为 `value`
- 可以自定义为其他名称

**使用场景：**
- 在条件判断中使用变量
- 在模板中引用变量
- 在Lambda函数中读取配置
- 传递数据到下一个节点

---

### 3. If-Else（条件分支）节点

**用途：** 根据条件选择不同的执行路径

**条件类型：**

#### 1. 表达式
最灵活的条件判断方式

**支持的运算符：**
- 比较：`>`, `<`, `==`, `!=`, `>=`, `<=`
- 逻辑：`&&`（且）, `||`（或）
- 括号：`()`

**示例：**
```javascript
// 简单比较
input.score > 80

// 多条件
input.score > 80 && input.status == "active"

// 复杂表达式
(input.score > 90 || input.bonus > 10) && input.verified == true
```

#### 2. 比较
结构化的比较配置

**配置项：**
- **左值**：要比较的值（如 `input.score`）
- **运算符**：
  - 等于 (==)
  - 不等于 (!=)
  - 大于 (>)
  - 大于等于 (>=)
  - 小于 (<)
  - 小于等于 (<=)
  - 包含
  - 开始于
  - 结束于
- **右值**：比较的目标值（如 `80`）

**示例：**
```
左值: input.score
运算符: 大于
右值: 80
```

#### 3. 存在性检查
检查字段是否存在且不为null/undefined

**配置项：**
- **检查字段路径**：如 `input.user.email`

**使用场景：**
- 验证必填字段
- 检查可选数据
- 防止空指针错误

#### 4. 类型检查
检查字段的数据类型

**支持的类型：**
- 字符串 (string)
- 数字 (number)
- 布尔值 (boolean)
- 对象 (object)
- 数组 (array)

**配置项：**
- **检查字段**：要检查的字段
- **期望类型**：期望的数据类型

---

### Else If 分支

**功能：** 支持多个条件判断

**如何使用：**
1. 点击"+ 添加 Else If"按钮
2. 输入条件表达式
3. 添加描述（可选）
4. 可以添加多个 Else If 分支

**示例：**
```
If: input.score > 90
  → 优秀路径

Else If #1: input.score > 80
  → 良好路径

Else If #2: input.score > 60
  → 及格路径

Else:
  → 不及格路径
```

---

## 🎨 变量管理面板

### 打开方式
点击顶部工具栏的"变量"按钮

### 功能

#### 1. 全局变量管理
- **添加变量**：点击"+ 添加"按钮
- **设置值**：直接在输入框中编辑
- **删除变量**：点击变量卡片右上角的"删除"

#### 2. 使用说明
- **设置变量**：使用 Set Variable 节点
- **获取变量**：使用 Get Variable 节点
- **引用变量**：在表达式中使用 `$变量名`

#### 3. 变量统计
实时显示：
- 全局变量数量
- Set Variable 节点数量
- Get Variable 节点数量
- If-Else 节点数量

---

## 📚 使用教程

### 教程1：基础变量使用

**场景：** 保存用户分数并根据分数给出评价

**步骤：**

1. **设置变量**
   ```
   节点1: Set Variable
   - 变量名称: user_score
   - 变量作用域: 工作流变量
   - 值来源: 输入数据
   - 输入路径: input.score
   ```

2. **获取变量**
   ```
   节点2: Get Variable
   - 变量名称: user_score
   - 默认值: 0
   - 输出字段名: score
   ```

3. **使用变量**
   ```
   节点3: If-Else
   - 条件: $user_score > 80
   - True → "优秀"
   - Else → "继续努力"
   ```

---

### 教程2：If-Else条件分支

**场景：** 根据用户等级提供不同服务

**步骤：**

1. **添加If-Else节点**
   ```
   条件类型: 比较
   左值: input.user_level
   运算符: 等于
   右值: "vip"
   ```

2. **添加Else If分支**
   ```
   Else If #1: input.user_level == "premium"
   Else If #2: input.user_level == "standard"
   ```

3. **连接节点**
   ```
   If-Else节点
     ├─ True → VIP服务节点
     ├─ Else If #1 → Premium服务节点
     ├─ Else If #2 → 标准服务节点
     └─ Else → 基础服务节点
   ```

---

### 教程3：复杂条件判断

**场景：** 审核系统 - 多条件综合判断

**工作流：**

```
Start
  ↓
Set Variable (保存审核数据)
  ├─ 变量: content_length = input.content.length
  ├─ 变量: keyword_count = input.keywords.length
  └─ 变量: user_trust_score = input.user.trust_score
  ↓
If-Else (综合判断)
  条件: ($content_length > 100 && $keyword_count < 5) && $user_trust_score > 80
  ├─ True → 自动通过
  └─ Else If: $user_trust_score > 50
      ├─ True → 人工审核
      └─ Else → 自动拒绝
```

---

### 教程4：动态阈值

**场景：** 根据时间段使用不同的评分标准

**工作流：**

```
Start
  ↓
Lambda (计算当前时间段)
  code: |
    const hour = new Date().getHours();
    return {
      is_peak_hour: hour >= 9 && hour <= 18
    };
  ↓
Set Variable (设置阈值)
  变量: score_threshold
  值来源: 表达式
  表达式: input.is_peak_hour ? 90 : 70
  ↓
Get Variable (获取阈值)
  变量名称: score_threshold
  ↓
If-Else (应用阈值)
  条件: input.user_score > $score_threshold
  ├─ True → 通过
  └─ Else → 不通过
```

---

## 🎯 常见场景

### 场景1：用户认证流程

```
Start
  ↓
HTTP Request (验证Token)
  ↓
Set Variable
  变量: is_authenticated
  值来源: 输入数据
  路径: input.response.authenticated
  ↓
Get Variable
  变量: is_authenticated
  ↓
If-Else
  条件: $is_authenticated == true
  ├─ True → 继续处理
  └─ Else → 返回错误
```

---

### 场景2：数据验证

```
Start
  ↓
If-Else (检查邮箱)
  条件类型: 存在性检查
  检查路径: input.user.email
  ├─ True → 继续
  └─ Else → 返回错误
  ↓
If-Else (检查邮箱格式)
  条件类型: 比较
  左值: input.user.email
  运算符: 包含
  右值: "@"
  ├─ True → 验证通过
  └─ Else → 格式错误
```

---

### 场景3：A/B测试

```
Start
  ↓
Lambda (随机分组)
  code: |
    return {
      group: Math.random() > 0.5 ? 'A' : 'B'
    };
  ↓
Set Variable
  变量: test_group
  值来源: 输入数据
  路径: input.group
  ↓
If-Else
  条件: $test_group == 'A'
  ├─ True → 方案A
  └─ Else → 方案B
```

---

### 场景4：多级审批

```
Start
  ↓
Set Variable (保存金额)
  变量: amount
  值来源: 输入数据
  路径: input.申请金额
  ↓
If-Else (金额判断)
  条件: $amount < 1000
  ├─ True → 自动批准
  └─ Else If: $amount < 10000
      ├─ True → 主管审批
      └─ Else If: $amount < 100000
          ├─ True → 经理审批
          └─ Else → 总监审批
```

---

## 💡 最佳实践

### 1. 变量命名
✅ **推荐：**
- 使用有意义的名称：`user_score`, `api_response`, `threshold_value`
- 使用下划线分隔：`user_name` 而不是 `userName`
- 保持一致性

❌ **避免：**
- 单字母：`a`, `x`, `temp`
- 拼音：`yonghu`, `fenshu`
- 过长：`this_is_a_very_long_variable_name_that_is_hard_to_read`

### 2. 变量作用域
✅ **推荐：**
- 优先使用工作流变量（全局）
- 只在必要时使用局部变量
- 在变量管理面板中预先定义常用变量

❌ **避免：**
- 过度使用局部变量
- 变量作用域混乱

### 3. 条件表达式
✅ **推荐：**
- 简单条件使用"比较"类型
- 复杂条件使用"表达式"类型
- 添加注释说明条件含义

❌ **避免：**
- 过于复杂的嵌套条件
- 没有注释的复杂表达式

### 4. If-Else分支
✅ **推荐：**
- 合理使用Else If，避免嵌套
- 为每个分支添加描述
- 考虑所有可能的情况

❌ **避免：**
- 过多的Else If（超过5个考虑使用Switch）
- 忘记处理Else情况

---

## 🔧 高级技巧

### 技巧1：变量引用

在表达式中引用变量：
```javascript
// 使用 $ 前缀
$user_score > $threshold

// 组合使用
input.value + $base_value * 2

// 在字符串中（需要模板节点）
"用户 {{$user_name}} 的分数是 {{$user_score}}"
```

### 技巧2：类型转换

在Lambda节点中转换变量类型：
```javascript
function process(input) {
  // 字符串转数字
  const score = parseInt($score_string);
  
  // 数字转字符串
  const scoreStr = $score_number.toString();
  
  // JSON解析
  const data = JSON.parse($json_string);
  
  return { score, scoreStr, data };
}
```

### 技巧3：条件链

使用多个If-Else节点创建决策树：
```
If-Else #1 (检查用户类型)
  ├─ VIP → If-Else #2 (检查VIP等级)
  │         ├─ 黄金 → 服务A
  │         └─ 白银 → 服务B
  └─ 普通 → If-Else #3 (检查积分)
            ├─ 高积分 → 服务C
            └─ 低积分 → 服务D
```

### 技巧4：默认值处理

使用Get Variable的默认值功能：
```
Get Variable
  变量名称: user_preference
  默认值: "default_theme"
  
// 如果变量不存在，自动使用默认值
// 避免工作流中断
```

---

## ❓ 常见问题

### Q1: 变量名有什么限制？

**A:** 变量名必须：
- 只包含字母、数字和下划线
- 不能以数字开头
- 区分大小写（`UserName` 和 `username` 是不同的变量）

### Q2: 全局变量和局部变量的区别？

**A:**
- **全局变量**：整个工作流都可以访问，适合共享数据
- **局部变量**：只在当前分支内有效，适合临时数据

### Q3: 如何在条件中使用变量？

**A:** 使用 `$` 前缀引用变量：
```javascript
$user_score > 80
$user_name == "admin"
$count * 2 > $threshold
```

### Q4: Else If 最多可以添加几个？

**A:** 理论上没有限制，但建议：
- 少于5个：使用If-Else节点
- 5-10个：考虑使用Switch节点
- 超过10个：考虑重构逻辑

### Q5: 如何调试条件判断？

**A:** 建议方法：
1. 使用Lambda节点输出变量值
2. 在变量管理面板查看变量
3. 简化条件逐步测试
4. 添加日志输出

### Q6: 变量可以存储什么类型的数据？

**A:** 支持所有JavaScript类型：
- 字符串：`"Hello"`
- 数字：`123`, `3.14`
- 布尔值：`true`, `false`
- 对象：`{"key": "value"}`
- 数组：`[1, 2, 3]`
- null/undefined

### Q7: 如何在多个节点间传递数据？

**A:** 两种方式：
1. **使用变量**（推荐）：
   ```
   节点A → Set Variable → 节点B → Get Variable
   ```

2. **直接连接**：
   ```
   节点A → 节点B (自动传递输出)
   ```

### Q8: 条件表达式支持哪些运算符？

**A:** 支持：
- 比较：`>`, `<`, `==`, `!=`, `>=`, `<=`
- 逻辑：`&&`（且）, `||`（或）, `!`（非）
- 算术：`+`, `-`, `*`, `/`, `%`
- 括号：`()`

---

## 📊 功能对比

| 功能 | 更新前 | 更新后 |
|------|--------|--------|
| 变量系统 | ❌ 无 | ✅ 完整支持 |
| 条件判断 | ⚠️ 简单Condition | ✅ If-Else-ElseIf |
| 变量管理 | ❌ 无 | ✅ 可视化面板 |
| 变量作用域 | ❌ 无 | ✅ 全局/局部 |
| 条件类型 | 1种 | 4种 |
| Else If支持 | ❌ 无 | ✅ 支持 |

---

## 🎉 总结

本次更新添加了工作流的核心基础功能：

### 新增节点（3个）
- ✅ Set Variable - 设置变量
- ✅ Get Variable - 获取变量
- ✅ If-Else - 条件分支（支持Else If）

### 新增功能
- ✅ 变量管理面板
- ✅ 全局/局部变量
- ✅ 4种条件类型
- ✅ 无限Else If分支
- ✅ 变量引用语法（$变量名）

### 使用场景
- ✅ 数据传递和共享
- ✅ 条件分支和决策
- ✅ 动态配置
- ✅ 状态管理
- ✅ 复杂业务逻辑

现在你可以创建更智能、更灵活的工作流！🚀

---

**更新时间：** 2025-12-29  
**版本：** v2.1.0  
**状态：** ✅ 已完成并测试通过

