# 快速启动指南

## 方式一：本地开发环境（推荐新手）

### 1. 准备工作

确保已安装：
- Go 1.21+
- Node.js 18+
- MySQL 8.0+

### 2. 创建数据库

```bash
# 登录MySQL
mysql -u root -p

# 创建数据库
CREATE DATABASE ai_chat CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 退出
exit
```

### 3. 配置后端

```bash
cd backend

# 创建配置文件
cat > .env << EOF
SERVER_PORT=8080
GIN_MODE=debug
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=你的MySQL密码
DB_NAME=ai_chat
JWT_SECRET=$(openssl rand -base64 32)
JWT_EXPIRE_HOURS=24
CORS_ORIGINS=http://localhost:3000
OPENROUTER_API_URL=https://openrouter.ai/api/v1
EOF

# 安装依赖
go mod download
```

### 4. 配置前端

```bash
cd ../frontend

# 创建配置文件
echo "REACT_APP_API_URL=http://localhost:8080/api" > .env

# 安装依赖
npm install
```

### 5. 启动服务

**终端1 - 启动后端：**
```bash
cd backend
go run main.go
```

看到 "Server is running on http://localhost:8080" 表示启动成功

**终端2 - 启动前端：**
```bash
cd frontend
npm start
```

浏览器会自动打开 http://localhost:3000

### 6. 开始使用

1. 注册账户
2. 登录系统
3. 进入"API配置"页面，添加OpenRouter API密钥
4. 进入"智能体"页面，创建你的第一个智能体
5. 进入"对话"页面，开始聊天！

---

## 方式二：使用Docker（推荐生产环境）

### 1. 确保已安装Docker和Docker Compose

```bash
docker --version
docker-compose --version
```

### 2. 启动所有服务

```bash
# 构建并启动
docker-compose up -d

# 查看日志
docker-compose logs -f
```

服务地址：
- 前端: http://localhost:3000
- 后端: http://localhost:8080
- MySQL: localhost:3306

### 3. 停止服务

```bash
docker-compose down
```

### 4. 完全清理（包括数据）

```bash
docker-compose down -v
```

---

## 方式三：使用启动脚本

```bash
# 给脚本执行权限
chmod +x start.sh

# 运行脚本
./start.sh
```

---

## 常见问题

### 1. 后端启动失败

**问题**: `Failed to connect to database`

**解决**:
```bash
# 检查MySQL是否运行
mysql -u root -p

# 检查数据库是否存在
SHOW DATABASES;

# 检查 .env 配置是否正确
cat backend/.env
```

### 2. 前端无法连接后端

**问题**: `Network Error` 或 `CORS Error`

**解决**:
```bash
# 确认后端正在运行
curl http://localhost:8080/health

# 应该返回: {"status":"ok"}

# 检查前端配置
cat frontend/.env
```

### 3. 端口被占用

**问题**: `address already in use`

**解决**:
```bash
# 查找占用8080端口的进程
lsof -i :8080

# 杀死进程
kill -9 <PID>

# 或者修改端口
# 编辑 backend/.env 中的 SERVER_PORT
```

### 4. Go依赖下载失败

**解决**:
```bash
# 设置Go代理
export GOPROXY=https://goproxy.cn,direct

# 重新下载
cd backend
go mod download
```

### 5. npm安装失败

**解决**:
```bash
# 清理缓存
npm cache clean --force

# 使用淘宝镜像
npm install --registry=https://registry.npmmirror.com

# 或使用yarn
yarn install
```

---

## 获取API密钥

### OpenRouter（推荐）

1. 访问 https://openrouter.ai/
2. 注册账户
3. 进入 https://openrouter.ai/keys
4. 点击"Create Key"创建API密钥
5. 充值（最低$5）
6. 在应用中添加API配置

**优势**：
- 支持300+模型
- 统一接口
- 按需付费
- 价格透明

### OpenAI

1. 访问 https://platform.openai.com/
2. 注册账户
3. 进入 https://platform.openai.com/api-keys
4. 创建API密钥

### Anthropic Claude

1. 访问 https://console.anthropic.com/
2. 注册账户
3. 获取API密钥

---

## 推荐的第一个智能体配置

```
名称: 通用助手
描述: 一个友好、专业的AI助手

系统提示词:
你是一个友好、专业的AI助手。
你应该：
1. 用清晰、准确的语言回答问题
2. 在不确定时承认不知道
3. 提供有用的建议和解决方案
4. 保持礼貌和专业

API配置: 选择你创建的OpenRouter配置
模型: anthropic/claude-3.5-sonnet
Temperature: 0.7
Max Tokens: 2000
```

---

## 下一步

- 查看完整文档: [README.md](README.md)
- 了解详细功能: [需求文档.md](需求文档.md)
- 查看项目总结: [项目完成总结.md](项目完成总结.md)

---

## 技术支持

遇到问题？
1. 查看上面的"常见问题"
2. 查看项目文档
3. 检查后端日志: `backend.log`
4. 检查浏览器控制台

---

**祝你使用愉快！** 🚀

